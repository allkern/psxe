name: macOS CI

on:
  push:
    branches: [ "actions" ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install SDL2
      run: brew install sdl2
    - name: make
      run: make
    - uses: actions/upload-artifact@v3
      with:
        name: binary
        path: bin/psxe