name: Ubuntu CI

on:
  push:
    branches: [ "actions" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: install-deps
      run: sudo apt update
      run: sudo apt install libsdl2-dev
    - name: make
      run: make